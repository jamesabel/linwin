[project]
name = "wslubuntugnome"
version = "0.1.0"
description = "TUI setup tool for WSL2 Ubuntu with WSLg GUI"
requires-python = ">=3.10"
dependencies = [
    "textual>=0.79.0",
]

[project.scripts]
setup-tui = "_setup_tui:main"
